# mock 또는 api 중 하나를 명시합니다.
VITE_DATA_SOURCE=mock

# 빈 값은 same-origin입니다. 교차 origin일 때만 https://api.example.com 형식으로 지정합니다.
VITE_API_BASE_URL=

# 로컬 api 개발에서 Vite proxy가 연결할 Backend origin입니다.
VITE_BACKEND_URL=http://localhost:8080
