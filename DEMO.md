# 교수자 Frontend API 연동 점검

교수자 Frontend는 별도 샘플 데이터 모드 없이 Backend API 응답으로만 동작합니다. API 실패 시 화면이 임의 데이터로 대체되지 않고 오류 또는 빈 상태를 표시해야 합니다.

## 실행

1. Backend를 `http://localhost:8080`에서 실행합니다.
2. Frontend에서 `pnpm dev`를 실행합니다.
3. 브라우저 개발자 도구의 Network 탭에서 요청 경로가 `/api/...` 또는 `/uploads/...`인지 확인합니다.
4. 로그인 후 교수자 화면의 각 기능을 실제 테스트 계정과 서버 데이터로 확인합니다.

별도 `.env` 파일은 필요하지 않습니다. 로컬 Vite 프록시는 `/api`와 `/uploads`를 `http://localhost:8080`으로 전달합니다.

## 기본 확인 항목

- 로그인·세션 복원·로그아웃이 Backend 응답과 일치한다.
- 학생 목록과 상세 화면이 동일한 학생 식별자를 사용한다.
- 훈련·검사·학습 현황·이야기·보고서 화면의 요청이 실패했을 때 샘플 데이터로 전환되지 않는다.
- 새로고침 후에도 Backend에 저장된 변경 사항이 유지된다.
- 401 응답 시 토큰 갱신 또는 로그인 화면 이동이 정상적으로 수행된다.
- 파일 및 이미지 URL이 `/uploads/...` 경로를 통해 표시된다.

## 배포 환경 확인

정적 Frontend와 Backend 앞에 reverse proxy를 두고 다음 경로를 연결합니다.

| Browser 요청 | 전달 대상 |
| --- | --- |
| `/api/...` | Backend API |
| `/uploads/...` | Backend 업로드 리소스 |

Frontend에는 Backend origin을 주입하지 않습니다. 배포 환경별 주소 차이는 reverse proxy 설정에서 관리합니다.

## 실패 판정

- Backend가 중지됐는데도 화면에 정상 데이터처럼 보이는 고정 데이터가 나타난다.
- 한 화면의 데이터가 다른 학생 또는 다른 훈련·검사 식별자로 요청된다.
- API 오류가 사용자에게 드러나지 않고 성공 상태로 처리된다.
- Frontend 빌드마다 Backend 주소를 바꾸기 위해 소스를 수정해야 한다.
